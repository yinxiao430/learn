package lanqiaobei14;

import java.util.Scanner;

public class Test4 {
	  public static void main(String[] args) {
	        Scanner scan = new Scanner(System.in);
	        //在此输入您的代码...
	        int n=scan.nextInt();
	        int x[]=new int [n];
	        for (int i = 0; i < n; i++) {
				x[i]=scan.nextInt();
			}
	        int doll[][]=new int [n-1][2];
	        for (int i = 0; i < n-1; i++) {
				for (int j = 0; j < 2; j++) {
					doll[i][j]=scan.nextInt();
				}
			}
	        double result=0;
	        for (int i = 0; i < n-1; i++) {
	        	double xzhou=x[i+1]-x[i];
	        	if (i!=n-2) {
					break;
				}
	        	double rdtime=time(doll[i][1], doll[i+1][1]);
				if (xzhou>rdtime) {
					result+=xzhou;
				}
				else {
					result+=rdtime;
				}
			}
	        System.out.printf("%.2f",result);
	        scan.close();
	    }
	  public static double time (int a1,int a2) {
		  Double rise=a1*0.7;
		  Double dive=a2*1.3;
		  
		return rise+dive;
	}
}
